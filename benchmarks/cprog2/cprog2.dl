//int x, y, z = 0;
//y = 10
//z = 20
//x = 30
//while(x > z){ z++ }
//while(z > y){ y++ }
//assert(x = y = z)

.decl low(x:number) input
.decl med(x:number) input
.decl high(x:number) input

.decl loop(i:number, x:number, y:number, z:number) output
.decl Res() output

loop(0, 0, 0, 0).
loop(1, x, l, z) :- loop(0, x, _, z),low(l).
loop(2, x, y, m) :- loop(1, x, y, _),med(m).
loop(3, h, y, z) :- loop(2, _, y, _), loop(1, _, _, z),high(h).
loop(4, x, y, z) :- loop(2, _, y, z), loop(3, x, y, z), x > z.
loop(4, x, y, z+1) :- loop(4, x, y, z), x > z.
loop(5, x, y, z) :- loop(4, x, y, z), x <= z.
loop(5, x, y+1, z) :- loop(5, x, y, z) , z > y.
loop(6, x, y, z) :- loop(5, x, y, z), z <= y.

Res() :- loop(6, x, y ,z), x = y, y = z.
